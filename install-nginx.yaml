---
- name: Install nginx and start service
  hosts: ip-192-168-95-212.ec2.internal
  remote_user: root
  become: yes
  tasks:
  - name: install nginx
    apt:
      name: nginx
      state: latest

  - name: install nginx
    service:
      name: nginx
      state: started
